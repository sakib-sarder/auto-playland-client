import useTitle from "../../../Hooks/useTitle";

const Blog = () => {
    useTitle("Blog")
    return (
        <div>
            <h1>Frequntly Ask Questions</h1>
        </div>
    );
};

export default Blog;